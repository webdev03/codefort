# Bun needs to be installed system-wide!
# Or alternatively add the /home/<user>/.bun directory to `neededDirs` in meta.json, and change this script to:
# /home/<user>/.bun/bin/bun run code.js
bun run code.js
